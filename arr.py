def main():
    scores = [10,20,30,40]
    total = sum(scores)
    average = total / len(scores)
    print("=== main/master branch output ===")
    print(f"Scores: {scores}")
    print(f"Sum: {total}")
    print(f"Average: {average}")
    print(f"minimum: {min(scroes)}")
    print(f"maximum: {max(scores)}")
if __name__ == "__main__":
    main()
